/**
 * FASE C LOCAL — Shell con tabs (sin login, sin Convex)
 * Guía: docs/GUIA-FASE-C-LOCAL.md (§2)
 */
// import { useState } from "react";
// import Capture from "./pages/Capture";
// import Receipts from "./pages/Receipts";
// import ReceiptDetail from "./pages/ReceiptDetail";

// TODO 1: type Tab = "capture" | "receipts"; estado tab (default "capture") y
//         selectedId: string | null (para mostrar el detalle en vez de la tab)

// TODO 2: layout con Tailwind (h-dvh flex-col): header con el título,
//         <main className="flex-1 overflow-y-auto"> que muestra:
//           - <ReceiptDetail> si hay selectedId
//           - si no, Capture o Receipts según la tab activa
//         + <nav> abajo con dos botones para cambiar de tab (resetear selectedId al cambiar)

function App() {
  return <p className="p-8 text-center">App sin implementar (Fase C LOCAL)</p>;
}

export default App
